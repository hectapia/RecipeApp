@page "/login"
@using System.ComponentModel.DataAnnotations

@inject NavigationManager Nav
@inject HttpClient Http

<h3>Login</h3>

<EditForm Model="@model" OnValidSubmit="@LoginAsync">
    <DataAnnotationsValidator />
    <ValidationSummary />

    <div>
        <label>Email</label>
        <InputText @bind-Value="model.Email" />
    </div>
    <div>
        <label>Password</label>
        <InputText @bind-Value="model.Password" Type="password" />
    </div>

    <button type="submit">Log in</button>
    @if (!string.IsNullOrEmpty(error))
    {
        <p style="color:red">@error</p>
    }
</EditForm>

@code {
    private LoginModel model = new();
    private string? error;

    public class LoginModel
    {
        [Required, EmailAddress] public string Email { get; set; } = string.Empty;
        [Required] public string Password { get; set; } = string.Empty;
    }

    private async Task LoginAsync()
    {
        try
        {
            // Build absolute URI using Nav.BaseUri
            var uri = new Uri($"{Nav.BaseUri}auth/login");
            var res = await Http.PostAsJsonAsync(uri, model);

            if (res.IsSuccessStatusCode)
            {
                Nav.NavigateTo("/recipes", forceLoad: true);
            }
            else
            {
                error = await res.Content.ReadAsStringAsync();
            }
        }
        catch (Exception ex)
        {
            error = ex.Message;
        }
    }
}
